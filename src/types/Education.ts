export type Education = {
    _id: string,
    institution: string,
    location: { city: string, province: string, country: string },
    program: string,
    credential: string
    field_of_study: string,
    start_date: string,
    end_date: string,
    status: string
}
